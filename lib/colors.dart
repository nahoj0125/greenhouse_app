import 'package:flutter/material.dart';

class AppColors {
  // App
  static const primary = Color.fromARGB(255, 23, 110, 15);

  // Connection status
  static const connected = Color(0xFF9FE1CB);
  static const disconnected = Color(0xFFF09595);

  // Temperature card
  static const tempBackground = Color(0xFFEAF3DE);
  static const tempBorder = Color(0xFFC0DD97);
  static const tempText = Color(0xFF27500A);
  static const tempIcon = Color(0xFF3B6D11);

  // Humidity card
  static const humidityBackground = Color(0xFFE6F1FB);
  static const humidityBorder = Color(0xFFB5D4F4);
  static const humidityText = Color(0xFF0C447C);
  static const humidityIcon = Color(0xFF185FA5);

  // Soil moisture card
  static const soilBackground = Color(0xFFFAEEDA);
  static const soilBorder = Color(0xFFFAC775);
  static const soilText = Color(0xFF633806);
  static const soilIcon = Color(0xFF854F0B);
}